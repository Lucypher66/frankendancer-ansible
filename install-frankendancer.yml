---
- name: Frankendancer Installation
  hosts: localhost
  become: true
  become_user: root
  gather_facts: true

  tasks:
    - name: Install rustup
      ansible.builtin.apt:
        name: "rustup"
        state: present